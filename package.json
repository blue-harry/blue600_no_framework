{
  "name": "server",
  "version": "1.0.0",
  "description": "dealer server",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon index",
    "start": "NODE_ENV=production node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "bluepen",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
